# list is an build in data type in Python that represents a collection of items.
# Lists are ordered, mutable (changeable), and allow duplicate values.

my_list = list(range(1,21))
print("Original list:", my_list)

my_list.append(21)
print("After appending 21:", my_list)
